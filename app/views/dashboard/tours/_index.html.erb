<div class="row">
  <div class="col-lg-12 col-md-12 col-xs-12 traffic">
    <div class="dashboard-list-box">
      <h4 class="gray">Your Tours</h4>
      <div class="table-box">
        <table class="basic-table">
          <thead>
          <tr>
            <th>Tour Name</th>
            <th>Bookings</th>
            <th>Destination</th>
            <th>Date</th>
            <th>Price</th>
            <th>Preview</th>
            <th>Edit</th>
            <th>Delete</th>
          </tr>
          </thead>
          <tbody>
          <% @tours.each do |tour| %>
          <tr id="tour-index-<%= tour.id %>">
            <td><%= tour.title %></td>
            <td class="t-id"><%= tour.bookings.count %></td>
            <td><%= tour.location.name %></td>
            <td><%= tour.date %></td>
            <td><%= tour.price %></td>
            <td> <a href="<%= tour_path(tour) %>"> <i class="fa fa-street-view"> </i> </a>  </td>
            <td><%= link_to "", dashboard_tour_tour_details_path(tour_id: tour.id), class: "fa fa-edit", remote: true %></td>
            <td><%= link_to "", dashboard_tour_path(tour), class: "fa fa-times", method: :delete, remote: true %></td>
          </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-lg-12 col-md-12 col-xs-12 traffic" id="tour_details_box">

  </div>
</div>